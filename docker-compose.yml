version: "3.9"

services:
  backend:
    build: .
    container_name: django_backend
    restart: always
    ports:
      - "8000:8000"
      # REMOVED: - "3000:3000"
    environment:
      # ... (environment variables unchanged)
      - DEBUG=False
      - SECRET_KEY=django-insecure-g7&n*5lth6bcg)0ui*row++lezc3fzd^-s$)ler+##o!adrcpj
      - ALLOWED_HOSTS=*
      - MEDIA_ROOT=/app/media
      - STATIC_ROOT=/app/static
      - SERVER_IP=0.0.0.0
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=wzjp4NUl+I8QthONqJKwnRL8cNFusnS0DtVG6n/hfug=
    volumes:
      - coolify_workspaces:/app/media/workspaces
      - coolify_static:/app/static
    # UPDATED: Use the new entrypoint script
    command: /entrypoint.sh
    depends_on:
      - redis

  # ... (redis service unchanged)